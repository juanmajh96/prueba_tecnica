name: prueba_tecnica_juan
description: Prueba de Desarrollo TüL

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  flutter_bloc: ^6.1.3
  dartz: ^0.9.2
  equatable: ^1.2.6
  cloud_firestore: ^0.16.0+1
  firebase_core: ^0.7.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.1
  bloc_test: ^7.1.0
  network_image_mock: ^1.1.0



flutter:

  uses-material-design: true
